/**
 * Pinázio Tipo B — PLACEHOLDER.
 *
 * O cálculo deste tipo ainda não foi definido. Quando estiver definido,
 * substitua o corpo de `calcular()` pela fórmula correta e ajuste o
 * breakdown para refletir os passos reais.
 *
 * Use pinazioPadrao.js como referência do formato esperado do retorno.
 */

export const pinazioTipoB = {
    id: 'tipoB',
    label: 'Pinázio Tipo B (a definir)',
    descricao: 'Cálculo a ser definido. Atualmente apenas placeholder.',
    larguraPinazio: 18, // ajustar quando definir o tipo real

    calcular(_input) {
        return {
            ok: false,
            erro: 'Cálculo do Pinázio Tipo B ainda não foi implementado. Escolha o Pinázio Padrão por enquanto.',
        };
    },
};
